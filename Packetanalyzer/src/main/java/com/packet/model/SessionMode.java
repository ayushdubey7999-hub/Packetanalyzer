package com.packet.model;

/** Display labels for the session mode indicator in the status bar. */
public final class SessionMode {

    public static final String LIVE_CAPTURE = "Live Capture";
    public static final String LOADED_SESSION = "Loaded Session";
    public static final String IMPORTED_CAPTURE = "Imported Capture";

    private SessionMode() {
    }
}
